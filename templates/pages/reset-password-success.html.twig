{% extends '/base.html.twig' %}
{% block body %}
    {% include '/partials/header.html.twig' %}
    <div id="content">
        <div class="content-box container">
            <div style="width: 500px; margin: 0 auto; margin-top: 120px; margin-bottom: 120px; padding: 30px; border-radius: 30px; background-color: #F45B69">
                <h2 style="text-align: center; font-family: europa;"><i class="fas fa-check"></i> Wachtwoord aangepast</h2>
                <a href="{{ path('login_page') }}" style="display: block; margin: 0 auto;">
                    <div class="btn btn-primary" style="display: block; margin: 0 auto;"><i class="fas fa-lock"></i> Direct inloggen</div>
                </a>
            </div>
        </div>
    </div>
    {% include '/partials/footer.html.twig' %}
{% endblock %}