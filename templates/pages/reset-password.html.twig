{% extends '/base.html.twig' %}
{% block body %}
    {% include '/partials/header.html.twig' %}
    <div id="content">
        <div class="content-box container">
            <div style="width: 400px; margin: 0 auto; margin-top: 120px; margin-bottom: 120px; padding: 30px; border-radius: 30px; background-color: #F45B69">
                <h2 style="text-align: center; font-family: europa;"><i class="fas fa-question"></i> Wachtwoord vergeten</h2>
                <div style="font-family: proxima-nova; margin: 15px 0; text-align: center;">Voer uw email-adres in om het reset-process te vervolgen:</div>
                {{ form_start(form) }}
                    {{ form_widget(form) }}
                <input type="submit" class="btn btn-primary" value="Wachtwoord resetten">
                {{ form_end(form) }}
            </div>
        </div>
    </div>
    {% include '/partials/footer.html.twig' %}
{% endblock %}